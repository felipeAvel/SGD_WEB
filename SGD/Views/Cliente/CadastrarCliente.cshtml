@model SGD.ViewModels.Clientes.InserirClienteViewModels

@{
    ViewBag.Title = "InserirCliente";
}

<div class="container" style="background-color:#f5f7fd">
    <div class="page-inner">

        <div class="page-header">
            <h3 class="fw-bold mb-3">Cadastrar/Cliente</h3>
            <ul class="breadcrumbs mb-3">
                <li class="nav-home">
                    <a href="@Url.Action("Index","Home")">
                        <i class="fa fa-home"></i>
                    </a>
                </li>
                <li class="separator">
                    <i class="fa-solid fa-arrow-right"></i>
                </li>
                <li class="nav-item">
                    <a href="@Url.Action("Index","Home")">Home</a>
                </li>
                <li class="separator">
                    <i class="fa-solid fa-arrow-right"></i>
                </li>
                <li class="nav-item">
                    <a href="@Url.Action("Index","Cliente")">Clientes</a>
                </li>
                <li class="separator">
                    <i class="fa-solid fa-arrow-right"></i>
                </li>
                <li class="nav-item">
                    <a href="@Url.Action("CadastrarCliente","Cliente")">Cadastrar Cliente</a>
                </li>
            </ul>
        </div>
        <div class="col-md-12">
            <div class="card card-round">
                <div class="card-header">
                    <div class="card-title">Dados</div>
                    <div class="card-category">
                        Preencha com todas as informações necessárias
                    </div>
                </div>
                <div class="card-body">
                    @*1-BLOCO-START*@
                    <div class="row">
                        <div class="col-md-6 col-lg-4">
                            <div class="form-group">
                                <label for="nome">Nome Cliente</label>
                                <input type="text" class="form-control" id="Nome" name="Nomev" placeholder="Obrigatório">
                                <small id="nomeHelp" class="form-text text-muted">
                                    Lembre-se de confirmar se o nome esta correto
                                </small>
                            </div>
                        </div>
                        <div class="col-md-6 col-lg-4">
                            <div class="form-group">
                                <label for="login">Telefone</label>
                                <input type="tel" class="form-control" id="Telefone" name="Telefone" placeholder="Obrigatório">
                                <small id="loginHelp" class="form-text text-muted">
                                    Para facilitar utilize o número do whatsapp
                                </small>
                            </div>
                        </div>
                        <div class="col-md-6 col-lg-4">
                            <div class="form-group">
                                <label for="email">E-mail</label>
                                <input type="email" class="form-control" id="Email" name="Email" placeholder="Obrigatório">
                                <small id="emailHelp" class="form-text text-muted">
                                    Utilize seu e-mail corporativo
                                </small>
                            </div>
                        </div>
                    </div>
                    @*1-BLOCO-START*@

                    @*2-BLOCO-START*@
                    <div class="row">
                        <div class="col-md-6 col-lg-4">
                            <div class="form-group">
                                <label for="racf">Cidade</label>
                                <input type="text" class="form-control" id="Cidade" name="Cidade" placeholder="Obrigatório">
                                <small id="racfHelp" class="form-text text-muted">
                                    Lembre-se de confirmar com o Cliente sua Cidade
                                </small>
                            </div>
                        </div>
                        <div class="col-md-6 col-lg-4">
                            <div class="form-group">
                                <label for="racf">CEP</label>
                                <input type="text" class="form-control" id="Cep" name="Cep" placeholder="Obrigatório">
                                <small id="racfHelp" class="form-text text-muted">
                                    Lembre-se de confirmar com o Cliente o CEP da Cidade
                                </small>
                            </div>
                        </div>
                        <div class="col-md-6 col-lg-4">
                            <div class="form-group">
                                <label for="racf">Endereço</label>
                                <input type="text" class="form-control" id="Endereco" name="Endereco" placeholder="Obrigatório">
                                <small id="racfHelp" class="form-text text-muted">
                                    Lembre-se de confirmar com o Cliente sua Cidade
                                </small>
                            </div>
                        </div>
                    </div>
                    @*2-BLOCO-END*@

                    @*3-BLOCO-START*@
                    <div class="row">
                        <div class="col-md-6 col-lg-4">
                            <div class="form-group">
                                <label for="foto">Foto</label>

                                <input type="file" accept=".png" class="form-control" id="Foto" name="Foto">

                                <small id="fotoHelp" class="form-text text-muted">
                                    Utilize uma imagem em .PNG
                                </small>
                            </div>
                        </div>
                    </div>
                    @*3-BLOCO-END*@
                </div>

                @*4-BLOCO-START*@
                <div class="card-action">
                    <button id="Inserir" class="btn btn-success">Cadastrar</button>
                    <a class="btn btn-danger" href="@Url.Action("Index","Cliente")">Cancelar</a>
                </div>
                @*4-BLOCO-END*@
            </div>
        </div>
    </div>
</div>

<script>
    $(document).ready(function () {

        var controllerInserirUsuario = '@Url.Action("InserirCliente", "Cliente")';
        $(document).on('click', '#Inserir', FuncInserir);

        function FuncInserir()
        {
            //Validação dos campos
            if ($('#Nome').val() == "") {
                Swal.fire({
                    icon: "error",
                    title: "Oops...",
                    text: "O campo Nome Completo é Obrigatorio!"
                });
            }
            else if ($('#Telefone').val() == "") {
                Swal.fire({
                    icon: "error",
                    title: "Oops...",
                    text: "O campo Telefone é Obrigatorio!"
                });
            }
            else if ($('#Email').val() == "") {
                Swal.fire({
                    icon: "error",
                    title: "Oops...",
                    text: "O campo E-mail é Obrigatorio!"
                });
            }
            else if ($('#Cidade').val() == "") {
                Swal.fire({
                    icon: "error",
                    title: "Oops...",
                    text: "O campo Cidade é Obrigatorio!"
                });
            }
            else if ($('#Cep').val() == "") {
                Swal.fire({
                    icon: "error",
                    title: "Oops...",
                    text: "O campo CEP é Obrigatorio!"
                });
            }
            else if ($('#Endereco').val() == "") {
                Swal.fire({
                    icon: "error",
                    title: "Oops...",
                    text: "O campo Endereço é Obrigatorio!"
                });
            }
            else if ($('#Foto').val() == "") {
                Swal.fire({
                    icon: "error",
                    title: "Oops...",
                    text: "O campo Funcional é Obrigatorio!"
                });
            }
            else {
                InserirCliente();
            }

            function InserirCliente() {
                var formData = new FormData();

                formData.append('nome', $('#Nome').val());
                formData.append('telefone', $('#Telefone').val());
                formData.append('email', $('#Email').val());
                formData.append('cidade', $('#Cidade').val());
                formData.append('cep', $('#Cep').val());
                formData.append('endereco', $('#Endereco').val());
                formData.append('foto', $('#Foto').prop('files')[0]);

                Swal.fire({
                    title: "Tem certeza?",
                    text: "Confirme para Cadastrar um novo Cliente!",
                    icon: "warning",
                    showCancelButton: true,
                    confirmButtonColor: "#3085d6",
                    cancelButtonColor: "#d33",
                    confirmButtonText: "Sim",
                    cancelButtonText: "Não",
                }).then((result) => {
                    if (result.isConfirmed) {
                        $.ajax({
                            url: controllerInserirUsuario,
                            type: 'POST',
                            data: formData,
                            processData: false,
                            contentType: false,
                            success: function (data) {
                                if (data == "Erro ao Inserir Cliente!") {
                                    Swal.fire({
                                        title: "Deleted!",
                                        text: data,
                                        icon: "success"
                                    });
                                }
                                else if (data == "Sucesso") {
                                    Swal.fire({
                                        position: "center",
                                        icon: "success",
                                        title: "Cliente Inserido com Sucesso",
                                        showConfirmButton: false,
                                        timer: 3500
                                    }).then(() => {
                                        window.location.href = '@Url.Action("Index", "Cliente")';
                                    });
                                }
                                else if (data == "Cliente ja Existente!") {
                                    Swal.fire({
                                        icon: "error",
                                        title: "Oops...",
                                        text: "O Cliente já Consta no Sistema!"
                                    });
                                }
                            },
                            error: function (request, status, error) {
                                console.log(request.responseText);
                            }
                        });
                    }
                });
            }
        }
    });
</script>